# Archived Components

These components are no longer used in the mobile app but are kept for reference.

## Archived on: February 10, 2026

### Components:

- **ChannelFilterBar.tsx** - Replaced by UnifiedFilterBar
- **DurationFilterBar.tsx** - Replaced by UnifiedFilterBar
- **FilterBar.tsx** - Replaced by UnifiedFilterBar
- **SortFilterBar.tsx** - Replaced by UnifiedFilterBar

### Reason:

All filter components were consolidated into a single `UnifiedFilterBar` component that handles sorting, channel filtering, and duration filtering in one unified interface.

### Safe to delete:

Yes, these can be permanently deleted if needed. They are kept only for reference.
